// we use this so we can quickly swiftly switch from 'uniforms-custom' npm import 
// to './src' when we need to try out changes or don't need cordova compatablity.

export * from './src' //'uniforms-custom' //'./src'